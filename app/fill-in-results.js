export default function fillInResult(element, data) {
    element.querySelector('.upper-title').innerHTML = data.show_title;
}

// Fill in the elements within the element argument using the data
// from the argument (see below for an example response)
